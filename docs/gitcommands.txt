
# make executable
git update-index --chmod=+x <file>

# make release
git checkout master
git pull

git merge develop

# build.gradle remove -SNAPSHOT

# build binaries
gradle clean build javadoc shadowJar

git add build.gradle
git commit -m "<release>"
git push

# check travis

# make release in github

git checkout develop
git merge master

# build.gradle next version -SNAPSHOT
git add build.gradle
git commit -m "<next-release>-SNAPSHOT"
git push
